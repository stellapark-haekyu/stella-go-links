{
  "name": "go-links",
  "version": "1.0.0",
  "description": "Internal go-links server (redirect short slugs to URLs)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
